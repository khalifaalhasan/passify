services:
  db:
    container_name: postgres-password-manager
    image: postgres:16.14-alpine3.23
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: